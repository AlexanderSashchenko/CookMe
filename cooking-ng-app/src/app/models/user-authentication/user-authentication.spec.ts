import {UserAuthenticationModel} from "./user-authentication.model";

describe('UserAuthenticationModel', () => {
  it('should create an instance', () => {
    expect(new UserAuthenticationModel()).toBeTruthy();
  });
});
