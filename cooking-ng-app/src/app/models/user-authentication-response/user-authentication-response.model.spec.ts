import {UserAuthenticationResponseModel} from "./user-authentication-response.model";

describe('UserAuthenticationResponseModel', () => {
  it('should create an instance', () => {
    expect(new UserAuthenticationResponseModel()).toBeTruthy();
  });
});
